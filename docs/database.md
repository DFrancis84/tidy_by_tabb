# Database

D1 database ID: `e0c5e80b-2105-4353-b0d3-bc408e9dbef8`

Migration `0001_clients_services.sql` creates normalized Clients and Services.
UUIDs are generated by the Worker. Prices use integer cents to avoid
floating-point currency errors.

Applied migrations are immutable. Every later schema change receives a new
numbered migration. Gallery remains outside D1 for this milestone.
